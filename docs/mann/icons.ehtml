<<manpage {projectgui(n) icons(n)} "Standard Athena Icons">>

<<section SYNOPSIS>>

<pre>
package require projectgui 1.0
</pre>

<<itemlist>>

<<section DESCRIPTION>>

This module defines a number of standard icon images.  Each icon is a
standard Tk image; all are defined in the <code>::projectgui::icon</code>
namespace.  <b>Note that it's necessary to use the fully qualified
name!</b>.<p>

Icons are generally square; each name is suffixed with the size of the
icon in pixels, e.g., <code>::projectgui::icon::pencil22</code> is a 22x22
pixel icon.<p>

<<section ICONS>>

The module defines the following icons.<p>

<<deflist icons>>

<<defitem pencil22 {::projectgui::icon::pencil22}>>

A pencil, i.e., "Edit".<p>

<<defitem plus22 {::projectgui::icon::plus22}>>

A bold plus sign, i.e., "Add".<p>

<<defitem x22 {::projectgui::icon::x22}>>

A large "X", i.e., "Cancel" or "Delete".<p>

<</deflist icons>>


<<section AUTHOR>>

Will Duquette<p>

<<section HISTORY>>

Original package.

<</manpage>>






